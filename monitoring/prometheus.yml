global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "predictive-api"
    metrics_path: /metrics

    static_configs:
      - targets:
          - predictive-backend-service.predictive-maintenance.svc.cluster.local:8000